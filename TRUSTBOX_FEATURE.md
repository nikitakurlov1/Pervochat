# Скарбничка довіри (Trust Box)

## 📋 Опис функціоналу

"Скарбничка довіри" - це анонімний канал зв'язку між учнями та директором школи. Учні можуть писати ідеї, скарги або запитання, а директор може відповідати на них, не знаючи хто автор.

## 🎯 Можливості

### Для учнів:
- ✅ Написати анонімне повідомлення директору
- ✅ Переглянути історію своїх повідомлень
- ✅ Отримати відповідь від директора
- ✅ Статус повідомлення (очікує відповіді / відповіли)

### Для директора (адміна):
- ✅ Переглянути всі повідомлення (анонімно)
- ✅ Відповісти на будь-яке повідомлення
- ✅ Бачити статистику (скільки очікують відповіді)
- ✅ Фільтрація за статусом

## 🔒 Анонімність

**Важливо:** Повідомлення зберігаються з прив'язкою до користувача в БД (щоб учень міг бачити свої повідомлення), але:
- API `/trustbox/all` НЕ повертає інформацію про автора
- Frontend адміна не показує ім'я або ID автора
- Замість імені показується "Анонімний учень"

## 🚀 Як користуватися

### Для учня:

1. Перейдіть у вкладку "Профіль"
2. Натисніть кнопку "Скарбничка довіри"
3. Натисніть "Написати директору"
4. Введіть ваше повідомлення
5. Натисніть "Відправити"
6. Очікуйте відповіді від директора

### Для директора:

1. Увійдіть як адмін (admin / admin1236)
2. У адмін-панелі натисніть вкладку "Повідомлення"
3. Перегляньте список повідомлень
4. Натисніть "Відповісти" на потрібному повідомленні
5. Напишіть відповідь
6. Натисніть "Відправити"

## 📊 База даних

### Модель TrustMessage

```prisma
model TrustMessage {
  id         Int      @id @default(autoincrement())
  userId     Int
  user       User     @relation(fields: [userId], references: [id])
  content    String
  reply      String?
  isAnswered Boolean  @default(false)
  createdAt  DateTime @default(now())
  repliedAt  DateTime?
}
```

## 🔌 API Endpoints

### POST `/api/trustbox`
Створити нове повідомлення (тільки для авторизованих користувачів)

**Body:**
```json
{
  "content": "Текст повідомлення"
}
```

### GET `/api/trustbox/my`
Отримати свої повідомлення (для учня)

**Response:**
```json
[
  {
    "id": 1,
    "content": "Моє питання",
    "reply": "Відповідь директора",
    "isAnswered": true,
    "createdAt": "2025-11-22T10:00:00Z",
    "repliedAt": "2025-11-22T11:00:00Z"
  }
]
```

### GET `/api/trustbox/all`
Отримати всі повідомлення (ТІЛЬКИ для адміна, без інформації про автора)

**Response:**
```json
[
  {
    "id": 1,
    "content": "Анонімне питання",
    "reply": null,
    "isAnswered": false,
    "createdAt": "2025-11-22T10:00:00Z",
    "repliedAt": null
  }
]
```

### PATCH `/api/trustbox/:id/reply`
Відповісти на повідомлення (ТІЛЬКИ для адміна)

**Body:**
```json
{
  "reply": "Текст відповіді"
}
```

## 🎨 Дизайн

### Стиль повідомлень:

**Повідомлення учня:**
- Вирівнювання: ліворуч
- Фон: сірий (#f0f2f5)
- Мітка: "Ваше повідомлення"

**Відповідь директора:**
- Вирівнювання: праворуч
- Фон: градієнт (фіолетовий)
- Мітка: "Відповідь директора" з іконкою щита
- Колір тексту: білий

### Іконки:
- Shield - захист, довіра
- MessageCircle - повідомлення
- Send - відправити

## 🔧 Налаштування

### 1. Оновити базу даних

```bash
npm run prisma:migrate
# Введіть назву: add_trustbox
```

### 2. Перезапустити сервер

```bash
npm run dev
```

### 3. Готово!

Функціонал працює одразу після міграції.

## 📱 Інтерфейс

### Учень:
- Кнопка в профілі: "Скарбничка довіри"
- Fullscreen модальне вікно
- Список своїх повідомлень
- Форма для нового повідомлення

### Адмін:
- Вкладка "Повідомлення" в адмін-панелі
- Список всіх повідомлень
- Статус кожного повідомлення
- Форма відповіді

## 🔐 Безпека

- Тільки авторизовані користувачі можуть створювати повідомлення
- Тільки адміни можуть бачити всі повідомлення
- Тільки адміни можуть відповідати
- Інформація про автора не передається на frontend адміна
- Спеціальний адмін (userId = 0) не може створювати повідомлення

## 💡 Приклади використання

### Учень пише питання:
```
"Чому в їдальні немає вегетаріанських страв?"
```

### Директор відповідає:
```
"Дякую за питання! Ми вже працюємо над розширенням меню. 
З наступного тижня з'являться вегетаріанські опції."
```

### Учень бачить:
- Своє питання (сірий фон)
- Відповідь директора (фіолетовий фон)
- Дати відправки та відповіді

## 🎯 Переваги

1. **Анонімність** - учні не бояться висловлюватися
2. **Прямий зв'язок** - швидка комунікація з керівництвом
3. **Історія** - всі повідомлення зберігаються
4. **Зручність** - простий інтерфейс
5. **Безпека** - захищені дані

## 📈 Статистика

Адмін бачить:
- Загальну кількість повідомлень
- Кількість необроблених повідомлень
- Дати створення та відповідей
