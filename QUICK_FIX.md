# Швидке виправлення помилки 500

## Проблема
Помилка 500 при спробі використати Скарбничку довіри.

## Причина
База даних не містить таблицю `TrustMessage`.

## Рішення

### ✅ Міграція вже застосована!

Міграція `add_trustbox` була успішно застосована.

### Наступні кроки:

1. **Перезапустіть сервер:**
   ```bash
   # Зупиніть поточний процес (Ctrl+C)
   npm run dev
   ```

2. **Перевірте роботу:**
   - Увійдіть як звичайний користувач
   - Перейдіть у Профіль
   - Натисніть "Скарбничка довіри"
   - Напишіть повідомлення

3. **Перевірте адмін-панель:**
   - Увійдіть як admin/admin1236
   - Перейдіть у вкладку "Повідомлення"
   - Побачите всі повідомлення

## Що було зроблено

1. ✅ Видалено enum UserRole (SQLite не підтримує enum)
2. ✅ Змінено тип role на String
3. ✅ Додано модель TrustMessage
4. ✅ Застосовано міграцію
5. ✅ Оновлено TypeScript типи

## Готово!

Тепер Скарбничка довіри працює! 🎉
