# Інструкція по запуску з адміністратором

## Швидкий старт

### 1. Встановлення залежностей
```bash
npm install
```

### 2. Налаштування бази даних
```bash
# Генерація Prisma Client
npm run prisma:generate

# Застосування міграцій
npm run prisma:migrate
# Введіть назву: add_user_roles

# Створення адміністратора
npm run prisma:seed
```

### 3. Запуск додатку
```bash
npm run dev
```

Додаток буде доступний на http://localhost:3000

## Облікові записи

### Адміністратор
- **Логін:** admin (або admin@school.com)
- **Пароль:** admin1236
- **Доступ:** Адмін-панель з можливістю видалення будь-якого контенту

### Звичайний користувач
Зареєструйтесь через форму реєстрації:
- Email: ваш email
- Ім'я користувача: ваше ім'я
- Пароль: ваш пароль

## Що нового в цій версії

### ✨ Функціонал адміністратора

1. **Ролі користувачів:**
   - USER (за замовчуванням)
   - ADMIN (повний доступ)

2. **Адмін-панель:**
   - Статистика по всьому додатку
   - Перегляд усіх постів
   - Видалення будь-якого поста
   - Видалення будь-якого коментаря
   - Червоні кнопки видалення для легкої ідентифікації

3. **Безпека:**
   - Перевірка ролі на backend
   - JWT токени з інформацією про роль
   - Захист API ендпоінтів

### 🎨 Дизайн

- Мінімалістичний інтерфейс
- SVG іконки (Lucide React)
- Адаптивний дизайн (Mobile-First)
- Українська мова інтерфейсу

## Структура проекту

```
school-messenger/
├── src/
│   ├── components/
│   │   ├── AdminPanel.tsx      # Адмін-панель
│   │   ├── Auth.tsx            # Авторизація
│   │   ├── Feed.tsx            # Стрічка постів
│   │   ├── CreatePost.tsx      # Створення поста
│   │   ├── Profile.tsx         # Профіль
│   │   └── ...
│   ├── store/
│   │   └── authStore.ts        # Zustand store з ролями
│   └── api/
│       └── api.ts              # API клієнт
├── server/
│   ├── routes/
│   │   ├── auth.ts             # Авторизація з ролями
│   │   ├── posts.ts            # CRUD постів (з перевіркою адміна)
│   │   ├── comments.ts         # CRUD коментарів (з перевіркою адміна)
│   │   └── ...
│   └── middleware/
│       └── auth.ts             # JWT middleware
├── prisma/
│   ├── schema.prisma           # Схема БД з ролями
│   └── seed.ts                 # Seed скрипт для адміна
└── uploads/                    # Завантажені файли
```

## Команди

### Розробка
```bash
npm run dev          # Запуск frontend + backend
npm run client       # Тільки frontend
npm run server       # Тільки backend
```

### База даних
```bash
npm run prisma:generate  # Генерація Prisma Client
npm run prisma:migrate   # Застосування міграцій
npm run prisma:seed      # Створення адміна
npx prisma studio        # GUI для БД
```

### Production
```bash
npm run build        # Збірка для production
```

## Можливі проблеми

### Помилка "Admin already exists"
Адміністратор вже створений. Це нормально.

### Помилка при міграції
```bash
# Видалити БД і створити заново
rm prisma/dev.db
npm run prisma:migrate
npm run prisma:seed
```

### Порт зайнятий
```bash
# Знайти процес
lsof -ti:3001

# Вбити процес
kill -9 <PID>
```

## Тестування

1. **Звичайний користувач:**
   - Зареєструйтесь
   - Створіть пост
   - Поставте лайк
   - Напишіть коментар
   - Спробуйте видалити чужий пост (не вийде)

2. **Адміністратор:**
   - Увійдіть як admin/admin1236
   - Побачите адмін-панель
   - Видаліть будь-який пост
   - Видаліть будь-який коментар
   - Перегляньте статистику

## Підтримка

Якщо виникли проблеми:
1. Перевірте, чи запущений сервер (порт 3001)
2. Перевірте, чи застосовані міграції
3. Перевірте, чи створений адміністратор
4. Перегляньте логи в консолі
